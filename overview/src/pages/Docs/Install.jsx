import React from 'react'
import Markdown from '../../components/markdowns/Markdown'

const Install = () => {
  return (
    <div>
      <Markdown filename={'install'} />
    </div>
  )
}

export default Install
